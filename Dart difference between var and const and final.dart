void main() {
  // The value is stored when the function is accessed/
  final name = "ahmed";
  print(name);
  // The value is stored when the program is running  print(age);
  const age = 15;
  print(age);
}
